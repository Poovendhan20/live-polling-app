import { useEffect,useState } from 'react'; import { Link } from 'react-router-dom'; import { getMyPolls } from '../api/polls';
export default function Dashboard(){const [polls,setPolls]=useState([]);useEffect(()=>{getMyPolls().then(r=>setPolls(r.data)).catch(()=>setPolls([]));},[]);return <main className="narrow-page"><span className="eyebrow">DASHBOARD</span><h1>Your polls, in one place.</h1><Link className="button" to="/create">Create a poll</Link>{polls.length>0&&<div className="poll-list">{polls.map(p=><Link key={p.id} to={`/results/${p.id}`} className="poll-item">{p.question}</Link>)}</div>}</main>}

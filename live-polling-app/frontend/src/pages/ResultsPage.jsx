import { useEffect,useState } from 'react'; import { useParams } from 'react-router-dom'; import { getPoll } from '../api/polls'; import useWebSocket from '../hooks/useWebSocket'; import PollResultsChart from '../components/PollResultsChart';
export default function ResultsPage(){const {id}=useParams();const [poll,setPoll]=useState();const live=useWebSocket(id);useEffect(()=>{getPoll(id).then(r=>setPoll(r.data))},[id]);useEffect(()=>{if(poll && Object.keys(live).length){setPoll(prev => prev ? { ...prev, counts: live } : prev)}},[live,poll]);if(!poll)return <main className="narrow-page"><p>Loading results...</p></main>;return <main className="narrow-page results"><span className="eyebrow">LIVE RESULTS</span><h1>{poll.poll.question}</h1><PollResultsChart options={poll.poll.options} counts={poll.counts}/><div className="share-box">Share this poll <strong>{window.location.origin}/vote/{id}</strong></div></main>}

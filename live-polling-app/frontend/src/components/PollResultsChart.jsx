export default function PollResultsChart({ options, counts = {} }) {
	const total = Object.values(counts).reduce((sum, count) => sum + Number(count || 0), 0);
	return <div className="results-list">{options.map((option, index) => { const count = Number(counts[index] || 0); const percentage = total ? count / total * 100 : 0; const width = total ? `${Math.max(4, percentage)}%` : '0%'; return <div className="result-row" key={option}><div className="result-row-header"><span className="result-option">{option}</span><span className="result-stat"><b>{count} votes</b><span>{percentage.toFixed(1)}%</span></span></div><div className="bar-track"><div className="bar-fill" style={{ width }} /></div></div>})}<small className="results-live-note">{total} total votes <span aria-hidden="true">·</span> updates live</small></div>;
}

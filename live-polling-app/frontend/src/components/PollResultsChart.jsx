export default function PollResultsChart({ options, counts = {} }) { const total = Object.values(counts).reduce((sum, count) => sum + count, 0); return <div className="chart">{options.map((option, index) => { const count = counts[index] || 0; const width = total ? `${Math.max(4, count / total * 100)}%` : '4%'; return <div className="bar-row" key={option}><div className="bar-label"><span>{option}</span><b>{count}</b></div><div className="bar-track"><div className="bar-fill" style={{ width }} /></div></div>})}<small>{total} total votes · updates live</small></div>; }

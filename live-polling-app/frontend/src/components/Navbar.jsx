import { useEffect, useState } from 'react'; import { Link } from 'react-router-dom'; import { getMyPolls } from '../api/polls'; import useAuth from '../hooks/useAuth';
export default function Navbar(){const {token,logout}=useAuth();const [hasCreatedPolls,setHasCreatedPolls]=useState(false);useEffect(()=>{if(!token){setHasCreatedPolls(false);return;}getMyPolls().then(r=>setHasCreatedPolls(Array.isArray(r.data)&&r.data.length>0)).catch(()=>setHasCreatedPolls(false));},[token]);return <nav><Link to="/choice" className="brand">Livepoll</Link><div>{token?<><Link to="/choice">Explore</Link>{hasCreatedPolls&&<Link to="/dashboard">Previously Created Polls</Link>}<button className="link-button" onClick={logout}>Sign out</button></>:<><Link to="/login">Log in</Link><Link to="/signup" className="button small">Get started</Link></>}</div></nav>}
